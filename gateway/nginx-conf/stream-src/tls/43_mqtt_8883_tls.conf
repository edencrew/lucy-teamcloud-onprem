server {
  listen 8883 ssl;

  # entrypoint가 READY일 때만 생성/복사해주는 공통 TLS include
  include /etc/nginx/stream.d/20_tls_stream.inc;

  set $mqtt_8883_upstream teamcloud-network-mqtt:1883;
  proxy_pass $mqtt_8883_upstream;

  proxy_timeout 1h;
  proxy_connect_timeout 10s;
}