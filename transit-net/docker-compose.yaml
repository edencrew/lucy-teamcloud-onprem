name: transit-net

services:
  keeper:
    image: alpine:3.20
    container_name: transit-net-keeper
    restart: unless-stopped
    command: ["sh", "-c", "trap : TERM INT; sleep infinity & wait"]
    networks:
      - tc-transit

networks:
  tc-transit:
    name: tc-transit
    driver: bridge